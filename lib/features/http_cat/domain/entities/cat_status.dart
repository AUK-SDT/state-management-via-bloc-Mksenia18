/// Domain entity that represents a cat status item in the app.
class CatStatus {
  final int statusCode;
  final String imageUrl;

  const CatStatus({required this.statusCode, required this.imageUrl});
}
